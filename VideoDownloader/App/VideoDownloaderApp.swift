import SwiftUI

@main
struct VideoDownloaderApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Video Downloader — scaffolding")
                .frame(minWidth: 600, minHeight: 400)
        }
    }
}
