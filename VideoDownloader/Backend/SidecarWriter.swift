import Foundation

/// TEMPORARY placeholder — real implementation lands in Task 16.
enum SidecarWriter {
    static func writeSidecar(
        forQueueItem item: QueueItem,
        libraryFolder: URL,
        ytdlpStdout: String
    ) async throws {
        // Implemented in Task 16
    }
}
